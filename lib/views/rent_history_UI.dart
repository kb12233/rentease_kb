import 'package:flutter/material.dart';

class RentHistoryUI extends StatelessWidget {
  const RentHistoryUI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}